from os.path import listdir, join

def combined_db(db_path):
    out_path = join(db_path, "combined")
    out_file = join(out_path, "database.json")
    files = os.


db_path ="/home/rice1902/Documents/hackathon/gml/db"